[project]
name = "simple-agent"
version = "0.1.0"
description = "Relatable Agentic Minion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "smolagents[gradio,litellm]>=1.12.0",
]
