name: voice_catalog
description: CLI tool for managing the Relagent voice-models.json catalog
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  args: ^2.7.0
  http: ^1.5.0
  path: ^1.9.1
  sherpa_voice:
    path: ../../dart_packages/sherpa_voice

dev_dependencies:
  test: ^1.25.0

executables:
  voice-catalog: voice_catalog
