name: sherpa_voice
description: >
  Shared sherpa-onnx domain knowledge: architecture taxonomy, model loader
  interface, config builders, and archive extractor. Pure Dart — no Flutter.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  archive: ^4.0.7
  path: ^1.9.1
  sherpa_onnx: ^1.12.17

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
