## 1. Engine: Event Store Module

- [x] 1.1 Create SQLite schema for events table (id, event_type, resource_path, item_id, created_at)
- [x] 1.2 Implement event store class with publish() method
- [x] 1.3 Implement get_events_after(last_id) for replay
- [x] 1.4 Implement TTL pruning (configurable, default 72h)
- [x] 1.5 Schedule periodic pruning in FastAPI lifespan (hourly)
- [x] 1.6 Add unit tests for event store

## 2. Engine: SSE Endpoint

- [x] 2.1 Add sse-starlette dependency (already present)
- [x] 2.2 Create `/api/v1/events` SSE endpoint
- [x] 2.3 Implement Last-Event-ID header parsing and replay
- [x] 2.4 Ping handled by sse-starlette (15s default)
- [x] 2.5 Document endpoint in OpenAPI schema (auto-generated by FastAPI)
- [x] 2.6 Add integration tests for SSE endpoint
- [x] 2.7 Fix: API event converter was using `latest_message_id` instead of `latest_sequence_id`

## 3. Engine: Event Publishing

- [x] 3.1 Publish event on session title generation/update
- [x] 3.2 Publish event on user message creation
- [x] 3.3 Publish event on assistant message creation
- [x] 3.4 Verify no duplicate events are published per operation (publish rejects events with existing id)
- [x] 3.5 Add integration tests for event publishing

## 4. Engine: Persistent Message IDs

- [x] 4.1 Add `id` field to UserMessage and AssistantMessage models
- [x] 4.2 Add `next_message_id` counter to ChatContext
- [x] 4.3 Implement `add_message()` and `add_messages()` helpers with auto-ID assignment
- [x] 4.4 Update services to use new add_message methods
- [x] 4.5 Add unit tests for message ID assignment

## 5. Engine: Messages Endpoint Enhancement

- [x] 5.1 Add `from_id` query parameter to `GET /api/v1/messages/{session_id}`
- [x] 5.2 Filter messages by persistent ID (not array index)
- [x] 5.3 Update OpenAPI schema with from_id parameter (auto-generated by FastAPI)
- [x] 5.4 Add unit tests for from_id filtering

## 6. App: SSE Client Provider

- [x] 6.1 Create SSE client service with reconnection logic
- [x] 6.2 Create Riverpod provider for SSE connection state
- [x] 6.3 Implement Last-Event-ID tracking and header inclusion
- [x] 6.4 Implement exponential backoff for reconnection
- [x] 6.5 Add unit tests for SSE client

## 7. App: Notification Handling

- [x] 7.1 Integrate SSE provider with chat provider
- [x] 7.2 Trigger message refresh on append events (using latest_sequence_id)
- [x] 7.3 Trigger session title refresh on update events
- [x] 7.3.1 Fix SSE client model to use latest_sequence_id instead of latest_message_id
- [x] 7.3.2 Fix SSE provider to pass fromId parameter to avoid duplicate fetching
- [ ] 7.4 Handle full state refresh on reconnection (optional safety measure)
- [ ] 7.5 Add integration tests for notification handling

## 8. Verification (SSE)

- [x] 8.1 Manual test: two clients see message updates
- [x] 8.2 Manual test: reconnection replays missed events
- [x] 8.3 Manual test: ping keeps connection alive
- [x] 8.4 Verify OpenAPI schema includes SSE endpoint and from_id parameter

## 9. App: Typed Event Model

- [x] 9.1 Create typed event classes (e.g., `SessionUpdatedEvent`, `MessagesAppendedEvent`) replacing generic `SseEvent`
- [x] 9.2 Each typed event carries only its relevant fields (no optional fields for other event kinds)
- [x] 9.3 Update SSE client to parse SSE messages into typed event objects
- [x] 9.4 Update SseProvider to handle typed events instead of generic SseEvent

## 10. App: Duplicate Event Deduplication

- [x] 10.1 Detect and skip duplicate update events when multiple are pending
- [x] 10.2 Process remaining events in order after deduplication
