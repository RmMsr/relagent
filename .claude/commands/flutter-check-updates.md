# Check for Updates

Perform a comprehensive check for available updates:

1. Check for Flutter dependency updates using `flutter pub outdated` in the apps directory
2. Report any packages that have newer versions available
3. Highlight any major version updates that might require migration work
4. Check if there are any security advisories for outdated packages
5. Provide a summary with recommendations on which updates to prioritize
